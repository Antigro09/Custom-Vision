# Local data

Calibration images, recordings, datasets, and generated smoke-test fixtures belong
here and are ignored by Git. Keep training/validation splits separate by recording
session and lighting conditions; adjacent frames in both splits inflate accuracy.
Use the eventual color intake camera for its own training dataset. Monochrome
images from the AprilTag camera may support early ball experiments.
