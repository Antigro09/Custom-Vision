# Model files

Keep training weights (`.pt`), portable exports (`.onnx`) and Jetson-built TensorRT
engines (`.engine`) here. Binaries are ignored by Git. No trained FRC model is bundled:
the game is undecided and no labeled camera dataset was provided. See
[object detection](../docs/objects.md) for the supported model contract.
